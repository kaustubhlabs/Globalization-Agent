# Skill: Intent-Driven Structural Mapping
1. Execute a browser-side script to extract only actionable selectors (`a`, `button`, `input`, `[role="button"]`).
2. Format the extraction as a lightweight JSON list.
3. Pass this tiny structural JSON file to the intent parser rather than reading raw elements or DOM trees.
